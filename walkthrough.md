# Gzadm Navigator · 初始化向导与注册模式优化更新

## 🌟 核心改进概览

### 1. 🔍 `gzhu init` 智能读取本地配置与跳过机制
* **自动识别历史配置**：启动时自动扫描 `.env`、`.env.local` 及 `data/system_providers.json`。
* **快速跳过与续用**：
  * `[1] 读取并基于现有配置更新 (默认)`：每一步按 `Enter` 回车即可保留历史填写的 API Key、Base URL 或服务配置。
  * `[2] 跳过配置并直接退出`：保留当前配置，直接启动应用。
  * `[3] 重新全新配置`：清空历史，重新录入。

---

### 2. 🎯 注册与登录方式单选互斥（三选一）
原多选/重叠选项已彻底重构为清晰互斥的单选模式：
```
【步骤 3/4】考生注册与登录方式选择 (三选一 · 互斥单选)：
  [1] 普通账号密码注册   (静态标准模式 · 账号名+密码 · 零第三方依赖)
  [2] 手机号验证码注册   (需手机 6 位短信验证码校验 · 接入腾讯云 SMS)
  [3] 邮箱验证码注册     (需邮箱 6 位验证码校验 · 接入 SMTP 邮件直发)
```
* **前端联动优化**：前端登录/注册卡片根据所选模式自动适配：
  * 普通模式：展示【账号登录】与【立即注册】；
  * 手机模式：展示【账号登录】与【手机号注册】（直接录入手机号并获取短信验证码）；
  * 邮箱模式：展示【账号登录】与【邮箱注册】（直接录入邮箱并获取邮件验证码）。

---

### 3. ✉️ 邮件 SMTP 服务深度整合
* 深度支持 QQ 邮箱、163 邮箱及自建 SMTP 服务器，支持 587/465 端口及 SSL/STARTTLS 协议。
