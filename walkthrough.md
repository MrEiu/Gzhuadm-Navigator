# 广州大学招生智能问答系统 - 高级注册、密码加密与用户管理重构成果

本次更新为 **Gzadm Navigator** 注入了完整的安全认证体系、高级验证码注册流程（腾讯云 SMS / SMTP 邮件）、Bcrypt 密码加盐杂凑加密以及全功能后台用户账号与信息管理大盘。

---

## 🌟 本次重构与功能亮点

### 1. 🛡️ 密码安全加密机制 (Node `bcryptjs`)
* **密码加盐杂凑存储**：所有新注册用户、重置密码及验证码注册用户的密码统一采用 `bcryptjs` 进行 10 轮加盐哈希运算，彻底告别明文存储。
* **平滑无感升级机制**：登录接口（`/api/auth/login`）具备向下兼容与自动升级能力——旧有明文账号登录成功后，系统会自动将其平滑升级转换为标准 `bcrypt` 杂凑密文。

---

### 2. 📲 高级验证码注册流程 (默认关闭，可自主开关)
* **后台可调控制开关**：在后台配置中心（Settings Tab）提供 **“高级验证码注册”** 开关。默认保持关闭，维持干净的标准账号密码注册；开启后前端自动解锁验证码注册界面。
* **多渠道验证支持**：
  * 📱 **手机号验证码 (支持腾讯云 SMS 接口/DevMock)**：提供手机号格式校验与 60 秒倒计时防刷新机制。
  * ✉️ **邮箱验证码 (支持 Node `nodemailer` SMTP)**：支持通过 QQ 邮箱、网易邮箱或校企 SMTP 主机直发验证码邮件。
  * ⏳ **5 分钟内存/Redis 校验与单次使用作废**。

---

### 3. 👥 后台管理 - 用户账号与资料深度管理 (Users Tab)
* **用户全量检索大盘**：支持按用户名、真实姓名、手机号、邮箱、高考省份进行模糊检索。
* **安全状态可视化**：实时展示每个考生的账号密文状态（`🔒 Bcrypt 杂凑加密` vs `⚠️ 基础存储`）以及密钥前缀预览。
* **全功能操作能力**：
  1. ✏️ **修改账号与资料 Modal**：管理员可直接修改考生的账号名、绑定手机号、邮箱号、高考分数、省份及 VIP 考生标识。
  2. 🔑 **重置安全密码 Modal**：支持管理员直接为考生重置新密码，并自动通过 `bcryptjs` 杂凑加密保存。
  3. 🧠 **查看考生偏好记忆**：快速调取该考生的 RAG 专属偏好档案。
  4. 🗑️ **账号注销与删除**：一键彻底注销指定考生账号。

---

## 🚀 验证与体验
1. 访问 `http://localhost:4173`。
2. 管理员登录 **`admin / admin123`**，前往后台 **系统配置** 即可开启“高级验证码注册”及配置腾讯云/SMTP API 密钥！
3. 前往后台 **用户管理**，即可实时体验用户资料编辑、密码查看与 Bcrypt 一键重置！
