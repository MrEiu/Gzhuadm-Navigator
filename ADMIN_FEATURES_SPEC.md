# 📋 Gzadm Navigator 后台管理系统功能全景清单与重构规范

> 本文档完整梳理并升级了管理后台的 **6 大业务模块、API 交互接口、数据流向与全局弹窗规范**。重做后台时将以此标准执行完全重写。

---

## 一、系统架构与顶层交互 (Admin Layout)

### 1. 认证安全与全局状态
- **身份判定**：仅允许 `role === 'admin'` 用户访问，普通考生拦截或重定向；
- **安全退出**：支持一键清理本地会话并返回登录界面；
- **全局状态条**：
  - 管理员身份与系统在线状态；
  - 当前生效的**默认对话主模型 (`DEFAULT_MODEL`)** 徽章；
  - 当前生效的**联网搜索引擎 (`SEARCH_PROVIDER`)** 徽章；
  - **全局手动刷新按钮**：一键重新拉取当前活动 Tab 的所有服务端最新数据。

### 2. 侧边栏导航 (Sidebar)
- 包含 6 大一级功能模块：
  1. `数据大盘 (Dashboard)`（展示当前应用的各项全局运行与流量数据，不包含知识库条目内容）
  2. `知识库管理 (RAG)`（带知识库条目数 Badge 徽标）
  3. `考生档案库 (Users)`（带注册考生数 Badge 徽标）
  4. `消息与词频 (Analytics)`
  5. `测试中心 (Playground)`
  6. `系统配置 (Settings)`

---

## 二、六大核心业务模块功能明细

### 1. 模块一：系统数据大盘 (Dashboard Tab) —— 纯净应用级监控
*注：彻底剥离知识库内容，聚焦全局应用运行、流量与考生特征。*
- **核心应用 KPI 监控卡片**：
  - **注册考生总数**：高分 VIP 考生占比与新增考生画像统计；
  - **问答交互总量**：今日咨询量与历史累计问答消息数；
  - **向量计算状态**：本地 BGE 512 维 ONNX 向量化引擎状态（0.1s 极速召回）；
  - **AI 模型网关状态**：当前主咨询模型与快速处理模型名称。
- **考生生源省份分布**：
  - 基于考生档案的 `province` 字段，统计 Top 5 热门生源省份（如广东、浙江、四川等）占比分布。
- **热门意向专业关注排行**：
  - 自动聚合考生咨询中提及频次最高的专业名称（如计算机、软件工程、人工智能等）。
- **系统基础设施健康度监控面板**：
  - `PostgreSQL pgvector` 向量数据库连接状态与响应延迟；
  - `Redis` 高速缓存状态与过期策略（TTL 30m）；
  - `ONNX Runtime` 模型加载状态；
  - `多源联网搜索` 自动容灾兜底状态。
- **常用管理快捷入口**：
  - 快速进入知识库管理、用户管理、测试中心、系统配置。

---

### 2. 模块二：知识库与 RAG 向量管理 (RAG Management Tab & Modals)
- **顶部数据指标栏**：已建档知识总数、结构化录取表格数、PNG 图片附件总数。
- **搜索与过滤栏**：
  - 知识标题 / 标签 / 正文实时搜索框；
  - 知识分类下拉单选过滤（全部 / 录取分数 / 宿舍环境 / 学费奖学金等）；
  - **卡片视图 (Cards)** 与 **表格视图 (Table)** 双模式无缝切换。
- **卡片视图展示**：
  - 分类标签、类型标识（表格/图片/文本）、标题、描述截断展示；
  - PNG 附件图片横向滑动预览与大图展示；
  - 检索关键词标签 Tag（#浙江, #计算机等）；
  - **命中关键词高亮预览**；
  - 单条知识编辑、删除操作。
- **表格视图展示**：结构化列表展示标题、分类、类型、附件数、标签集合与操作列。
- **单条知识录入 / 编辑弹窗 (`RagItemModal`)**：
  - 标题、分类、数据类型（文本 / 表格 / 图片）；
  - 描述正文多行输入，**实时显示切片字数与 Token 预估指示器**；
  - **PNG 图片附件上传管理**：上传本地图片至服务端 `/public/uploads` 并转存 URL，实时挂载至该知识节点；
  - 向量检索关键词与标签输入（逗号自动分隔）。
- **AI 文档智能切片与批量导入弹窗 (`DocChunkImportModal`)**：
  - **两种导入格式**：文本/文档（TXT, MD, DOC, DOCX）与表格（CSV, JSON）；
  - **三种切片引擎**：
    1. **智能语义切片**：调用 `FAST_MODEL` 自动提炼 3~15 个语义独立切片、分类与向量关键词；
    2. **Markdown 标题章节**：按 Markdown 标题或章节自动切分；
    3. **固定字数切片**：按 400 字滑动窗口切分。
  - **切片列表预览与微调**：支持在入库前对每一个切片进行单独在线修改 (`ChunkSingleEditor`) 或删除；
  - **一键批量保存**：一键写入知识库并自动调用 BGE 模型生成 512 维向量入库。

---

### 3. 模块三：考生档案库与 VIP 策略管理 (Users Tab & Modals)
- **策略阈值控制台**：
  - **压线风险拦截阈值**（300 ~ 600 分）：低于该分数的考生触发智能熔断/拒绝服务提示，保护高峰期算力；
  - **VIP 专属定制通道阈值**（500 ~ 700 分）：高于该分数的考生自动开启 VIP 高分位次深度方案；
  - **分流策略一键启用 / 暂停开关**。
- **考生列表检索栏**：
  - 用户名 / 真实姓名 / 高考省份实时过滤搜索；
  - 注册考生总数统计。
- **考生画像卡片**：
  - 账号名、真实姓名、头像；
  - VIP 身份标记与「设为 VIP / 取消 VIP」一键切换；
  - **成员角色管理**：支持一键将普通用户提拔为管理员 (`Admin`) 或降为普通考生 (`User`)；
  - 高考核心档案：省份、分数、全省排名位次、选科组合、特殊意向说明、注册时间；
  - **一键穿透查看该考生的完整对话历史**（弹窗回放该考生与 AI 的所有历史对话记录）；
  - 考生资料修改按钮、密码强制重置按钮。
- **管理员修改考生资料弹窗 (`AdminEditUserModal`)**：
  - 强制更新姓名、高考省份、总分、位次、选科、手机号码、电子邮箱。
- **管理员强制重置密码弹窗 (`AdminResetPasswordModal`)**：
  - 强制设置考生新密码，后端自动执行 Bcrypt 10 轮加盐哈希更新；
  - **提供一键生成高强度随机临时密码按钮**（支持一键复制）。
- **考生专属偏好记忆库弹窗 (`PersonalRagModal`)**：
  - 查看 AI 顾问在对话中为该考生自动提炼沉淀的专属记忆节点（专业倾向、家庭考量、意向城市等）。

---

### 4. 模块四：咨询意向与高频词热点分析 (Analytics Tab)
- **增量关键词热点分析**：
  - 自动基于历史问答数据增量分词统计，展示 **Top 15 高频热词气泡**（如 `#录取线`、`#专业`、`#宿舍`、`#学费`、`#计算机`、`#转专业` 等及出现频次）；
  - **热词联动过滤**：点击任意热词气泡，下方问答明细列表立即联动筛选包含该词的对话。
- **全网考生问答记录明细**：
  - 问答内容全局即时搜索框；
  - **时间范围筛选器**（全部 / 今日 / 最近7天）；
  - **全功能 Markdown 富文本渲染**：完美呈现回答中的数据表格排版、加粗颜色、引用说明、实景图片与外链；
  - **一键复制回答内容**；
  - **一键提炼沉淀至知识库**：发现高质量回答可一键转存为标准 RAG 知识条目。

---

### 5. 模块五：检索与联网测试诊断中心 (Playground Tab)
- **子模块 A：校方 RAG 检索精准度与自适应截断诊断**：
  - 快速测试查询 Chip 推荐（如“浙江 计算机 分数线”、“宿舍四人间 空调 独卫”等）；
  - 检索词输入与执行；
  - **向量得分可解释性拆解**：展示稠密向量余弦分、Token 词频匹配加权、省份实体加权的明细得分计算过程；
  - 诊断结果卡片：综合匹配得分、命中标题/分类、文本内容、图片预览、**自适应截断过滤生效状态**。
- **子模块 B：全网多源搜索引擎实时测试**：
  - 引擎下拉切换：`DuckDuckGo (免Key默认)` / `Tavily (AI原生搜索)` / `博查 AI (国内政策优化)`；
  - 推荐热搜词 Chip；
  - 实时抓取测试、毫秒级耗时统计（`elapsedMs`）；
  - 抓取清洗结果卡片：标题外链、摘要正文、来源引擎、原始 URL。
- **多源结果一键对比模式**：
  - 支持对同一问题并排对比「校内 RAG 知识召回」与「互联网实时搜索结果」。

---

### 6. 模块六：系统模型与引擎配置 (Settings Tab)
- **大模型服务商快捷预设卡片**：
  - 一键填充预设：`DeepSeek`、`OpenAI Official`、`阿里通义千问`、`硅基流动`、`智谱清言 GLM`、`月之暗面 Kimi`、`OpenAI 兼容本地网关`。
- **AI 网关与双模型分配**：
  - 接口 Base URL；
  - API Key（带密文掩码保护）；
  - **API 连通性实时测试按钮 (Test Connection)**：一键探活连通性与网络延迟；
  - **默认主对话模型 (`DEFAULT_MODEL`)**：用于考生问答；
  - **快速处理模型 (`FAST_MODEL`)**：用于文档语义切片与后台分析；
  - **一键拉取远程可用模型列表**：实时调用 `/api/admin/models` 获取该厂商支持的模型列表供下拉选择。
- **AI 顾问系统提示词 (Prompt) 在线自定义**：
  - 支持在线查看并自定义 Dr. Elena 咨询顾问的人设、表达风格与决策规则。
- **联网搜索引擎选配**：
  - 选择默认引擎（DuckDuckGo / Tavily / 博查）；
  - 填报 Tavily API Key / 博查 API Key。
- **考生注册方式与安全通道**：
  - 注册模式切换（普通账号密码 / 手机短信 / 邮箱验证码）；
  - 腾讯云 SMS 参数配置（SecretId, SecretKey, SdkAppId, SignName, TemplateId）；
  - SMTP 邮箱服务配置（Host, Port, User, Pass）。
- **一键保存并热生效**：
  - 写入 `.env` 并即时刷新全局生效。

---

## 三、前后端 API 接口清单对照表

| 序号 | 接口路径 | 请求方式 | 功能用途 |
| :--- | :--- | :--- | :--- |
| 1 | `/api/admin/dashboard-stats` | `GET` | 获取大盘应用流量、生源省份分布、基础设施健康状态 |
| 2 | `/api/admin/rag/items` 或 `/api/admin/rag` | `GET` | 获取全量 RAG 知识库条目列表 |
| 3 | `/api/admin/rag` | `POST` | 新增单条知识条目 |
| 4 | `/api/admin/rag/:id` | `PUT` | 修改单条知识条目 |
| 5 | `/api/admin/rag/:id` 或 `/api/admin/rag/items/:id` | `DELETE` | 删除单条知识条目 |
| 6 | `/api/admin/rag/batch` | `POST` | 批量保存智能切片数据入库并向量化 |
| 7 | `/api/admin/parse-document` | `POST` | 文档 智能语义/Markdown标题/固定字数切片解析 |
| 8 | `/api/admin/parse-table` | `POST` | CSV / JSON 表格结构化解析 |
| 9 | `/api/admin/upload-image` | `POST` | PNG 图片附件上传并返回访问链接 |
| 10 | `/api/admin/rag/search` | `POST` | RAG 检索精准度测试与自适应截断诊断 |
| 11 | `/api/admin/users` | `GET` | 获取注册考生列表、角色权限与高考画像数据 |
| 12 | `/api/admin/users/update` | `POST` | 管理员修改考生资料、切换管理员角色或强制重置密码 |
| 13 | `/api/admin/users/delete` | `POST` | 删除考生账号 |
| 14 | `/api/user/sessions` | `GET` | 按考生用户名穿透拉取该考生的全部历史对话记录 |
| 15 | `/api/admin/config` | `GET` | 读取系统环境变量与模型网关配置 |
| 16 | `/api/admin/config` | `POST` | 保存并写入新配置到 `.env` 文件 |
| 17 | `/api/admin/models` | `GET` | 实时拉取大模型服务商可用模型列表与连通性检测 |
| 18 | `/api/admin/web-search` | `POST` | 全网实时联网搜索测试 |
| 19 | `/api/admin/word-analytics` | `GET` / `POST` | 读取与同步考生高频词统计数据 |
